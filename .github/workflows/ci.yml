name: CI

on: [push, pull_request]

jobs:
  tests:
    name: "Citizen Science Task Bounties - Categories System"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Check contract syntax"
        uses: docker://hirosystems/clarinet:latest
        with:
          args: check

      - name: "Setup Node.js"
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: "Install dependencies"
        run: npm ci

      - name: "Run tests"
        run: npm test tests/categories-basic.test.ts